# Product Backlog de Licican

## Objetivo
Traducir la vision de Licican en trabajo priorizado, trazable y ejecutable para `developer-teams`, manteniendo alineacion con la validacion posterior de `qa-teams`.

## Escala de prioridad
- `P0`: imprescindible para validar el MVP de descubrimiento.
- `P1`: alto valor tras validar el MVP de descubrimiento.
- `P2`: mejora relevante, util para aprendizaje o escalado, pero no bloqueante.

## Estado operativo comun
- `nuevo`: item preparado por producto y disponible para que `developer-teams` lo tome.
- `en desarrollo`: item tomado por `developer-teams`.
- `listo para qa`: item implementado y pendiente de validacion funcional.
- `no validado`: item revisado por `qa-teams` con hallazgos bloqueantes o incumplimientos.
- `validado`: item aceptado por `qa-teams`.
- `cerrado`: item validado, integrado y cerrado administrativamente por `product-manager`.

## Estado de referencia de backlog
- Fecha de corte funcional: 2026-04-03.
- `PB-002` queda cerrado administrativamente: `qa-teams` lo valido en la issue #4 y su entrega se integrara en `main` junto con `PB-001` desde la rama tecnica ya validada.
- `PB-001` queda cerrado administrativamente: `qa-teams` lo valido en la issue #3 y su entrega base ya viene incluida en la rama validada de `PB-002`.
- `PB-007` queda cerrado administrativamente: su alcance fue validado por `qa-teams` y esta integrado en `main`.
- `PB-006` queda cerrado administrativamente: `qa-teams` lo revalido en la issue #2 y la rama `feat/pb-006-clasificacion-ti-auditable` ya esta integrada en `main`.
- `PB-003` queda cerrado administrativamente: `qa-teams` valido la reentrega en la issue #5 el 2026-03-20, `developer-teams` integro la rama en `main` el 2026-03-21 y `product-manager` cierra la issue tras verificar esa integracion.
- En esta revision quedan cerradas dos decisiones de producto que ya no deben tratarse como preguntas abiertas:
  - Regla funcional para expedientes mixtos TI.
  - Tratamiento funcional minimo de oportunidades anuladas, desiertas, desistidas o modificadas.
- Se incorpora `PB-009` para priorizar de forma explicita la recopilacion desde fuentes reales oficiales nominadas; esta necesidad no quedaba cubierta por `PB-007`, que solo delimitaba cobertura funcional.
- `PB-009` queda cerrado administrativamente: `qa-teams` valido la entrega integrada en `main` en la issue #9 el 2026-03-24, `developer-teams` dejo constancia de fusion y borrado de rama el mismo dia y `product-manager` cerro la issue el 2026-03-25.
- Se incorpora `PB-010` para fijar una base de navegacion principal y adaptabilidad responsive antes de seguir ampliando modulos sin una estructura comun estable.
- Se incorporan `PB-011` y `PB-012` para convertir en prioridad ejecutable la carga desde los ficheros `.atom` presentes en `data/`, la consolidacion de snapshots versionados y la exposicion funcional alineada con el Excel `data/licitaciones_ti_canarias.xlsx`.
- `PB-004` queda cerrado administrativamente: `qa-teams` valido la entrega en la issue #6 el 2026-03-25 y `developer-teams` dejo constancia de fusion en `main` y borrado de rama el 2026-03-26.
- `PB-011` queda cerrado administrativamente: `qa-teams` valido la entrega en la issue #11 el 2026-03-27, `developer-teams` la integro en `main` y elimino la rama tecnica el 2026-03-28, y `product-manager` cerro la issue el mismo dia.
- `PB-012` queda cerrado administrativamente: `qa-teams` valido la reentrega en la issue #12 el 2026-03-29, `developer-teams` la integro en `main` y elimino la rama tecnica el 2026-03-30, y `product-manager` cierra la issue tras verificar la integracion.
- Se incorpora `PB-013` para convertir en trabajo ejecutable el modelo funcional de roles y permisos definido ya en la especificacion consolidada del producto.
- Se incorpora `T-001` como trabajo tecnico de la issue #14 para sustituir la fuente de datos actual por lectura operativa desde PostgreSQL sobre la tabla `licitacion`; la entrega ya fue validada por `qa-teams`, integrada en `main` y cerrada administrativamente por `product-manager`.
- Se incorpora `PB-014` para paginar los resultados del catalogo y de la API cuando el volumen de licitaciones crece y el usuario necesita navegar por bloques manejables.
- Se incorpora `PB-015` para ofrecer un panel de control de conservacion y archivado que permita parametrizar la antiguedad de almacenamiento de las licitaciones.
- Se incorpora `PB-016` para ofrecer un modulo de gestion de usuarios y accesos con trazabilidad, control de roles y baja logica de cuentas.
- Se incorpora `T-002` para corregir la regresion del filtrado tras la migracion a PostgreSQL y restablecer la busqueda funcional sobre el catalogo.
- Se incorporan `T-003` a `T-012` como una secuencia tecnica de refactorizacion sin cambio de comportamiento externo para reducir deuda estructural, separar responsabilidades en `app.py` y reforzar mantenibilidad antes de nuevas expansiones funcionales.
- `PB-005` queda cerrado administrativamente: `qa-teams` lo valido en la issue #7 el 2026-03-31 y la issue fue cerrada en GitHub el 2026-04-01.
- `PB-013` queda cerrado administrativamente: la issue #13 fue cerrada en GitHub el 2026-04-01 y deja de figurar como trabajo funcional abierto.
- `PB-016` queda cerrado administrativamente: `qa-teams` lo valido en la issue #28 el 2026-04-02, la capacidad ya esta integrada en `main` y `product-manager` reconcilia su cierre administrativo el 2026-04-03.

## Backlog priorizado

| ID | Titulo | Descripcion | Prioridad | Valor de negocio | Criterios de aceptacion | Dependencias | Estado | Trazabilidad |
|---|---|---|---|---|---|---|---|---|
| PB-001 | Catalogo inicial de oportunidades TI de Canarias | El usuario debe poder consultar en un unico listado las licitaciones TI detectadas dentro de la cobertura MVP ya delimitada. | P0 | Permite validar la propuesta central de centralizacion y descubrimiento temprano. | 1. Existe un listado consultable de oportunidades TI. 2. Cada oportunidad muestra al menos titulo, organismo, ubicacion, presupuesto si existe, fecha limite y estado oficial del expediente si la fuente lo informa. 3. Solo se muestran oportunidades etiquetadas como TI segun reglas funcionales vigentes. 4. El usuario puede distinguir la fuente oficial de cada registro. 5. Si no hay oportunidades disponibles, se muestra un estado vacio claro. | PB-007, PB-006 | `cerrado` | HU-01, CU-01, issue #3 |
| PB-002 | Ficha de detalle de licitacion | El usuario debe acceder a una vista con la informacion critica de cada oportunidad para decidir si merece seguimiento. | P0 | Reduce tiempo de analisis y evita revisar manualmente el pliego completo para un primer filtro. | 1. Desde el catalogo se accede al detalle. 2. La ficha muestra presupuesto, plazo, procedimiento, criterios de adjudicacion, solvencia tecnica si esta disponible y enlace a la fuente oficial. 3. El usuario puede identificar la fecha limite, el organismo convocante y el estado oficial del expediente sin ambiguedad. 4. Si un dato no esta disponible en origen, se muestra como no informado. 5. Si la oportunidad fue modificada y la fuente publica nueva fecha limite u otra variable critica, la ficha muestra el ultimo dato oficial disponible. | PB-001 | `cerrado` | HU-02, CU-02, issue #4 |
| PB-003 | Filtros funcionales de busqueda | El usuario debe poder filtrar oportunidades por palabras clave, presupuesto, procedimiento y ubicacion. | P0 | Aumenta relevancia y hace util el volumen de datos agregado. | 1. El usuario puede aplicar filtros por palabra clave, rango de presupuesto, procedimiento y ubicacion. 2. El listado refleja los filtros activos. 3. El usuario puede limpiar los filtros. 4. Si no hay resultados, se muestra un estado vacio comprensible. 5. Si el usuario introduce un rango de presupuesto invalido, el sistema solicita corregirlo y no lo interpreta como una busqueda valida sin resultados. | PB-001 | `cerrado` | HU-03, CU-03, issue #5 |
| PB-006 | Reglas funcionales de clasificacion TI | El producto debe establecer reglas verificables para decidir si una oportunidad es relevante para tecnologia antes de ampliar el catalogo. | P0 | Reduce falsos positivos, evita debates posteriores con QA y protege la utilidad del catalogo. | 1. Existe un criterio funcional documentado de inclusion TI. 2. El criterio contempla CPVs, palabras clave y casos frontera. 3. Las exclusiones relevantes quedan explicitadas. 4. `qa-teams` puede contrastar una muestra representativa con estas reglas desde una superficie funcional verificable. | PB-007 | `cerrado` | HU-06, CU-08, issue #2 |
| PB-007 | Cobertura inicial de fuentes prioritarias | El MVP debe delimitar que fuentes oficiales entran en la primera entrega para evitar ambiguedad de alcance y expectativas incorrectas. | P0 | Permite planificar entrega incremental y medir cobertura real. | 1. Existe una lista priorizada de fuentes objetivo para la primera release. 2. Cada fuente tiene estado de inclusion objetivo: `MVP`, `Posterior` o `Por definir`. 3. La definicion queda alineada con roadmap y backlog. 4. La entrega no induce a pensar que existe cobertura total del ecosistema canario. | Ninguna | `cerrado` | HU-07, CU-06, issue #1 |
| PB-009 | Priorizacion de recopilacion desde fuentes reales oficiales | El producto debe priorizar que la recopilacion de contratos y concursos se haga sobre fuentes oficiales reales y verificables, empezando por boletines y portales con valor temprano para Canarias. | P0 | Refuerza la credibilidad del catalogo, reduce riesgo de datos irreales o desactualizados y acerca antes el producto a la propuesta de valor comercial. | 1. Existe una lista priorizada y ordenada de fuentes reales oficiales para recopilacion temprana. 2. La prioridad nombra de forma explicita `BOC` (`https://www.gobiernodecanarias.org/boc/`), `BOP Las Palmas` (`https://www.boplaspalmas.net/nbop2/index.php`) y `BOE` (`https://www.boe.es/`) y las clasifica como `Ola 1`, `Ola 2` o `Ola 3`. 3. La primera entrega de esta prioridad deja visible en una superficie verificable que cada oportunidad recopilada conserva, como minimo, fuente oficial, fecha de publicacion o equivalente, enlace oficial y estado oficial si la fuente lo informa. 4. La documentacion funcional deja explicito que alertas, pipeline y ampliacion de cobertura a nuevas administraciones quedan fuera de esta iteracion. 5. Existe al menos una issue ejecutable en GitHub para que `developer-teams` implemente esta prioridad con trazabilidad a `PB-007` y `PB-006`. | PB-007, PB-006 | `cerrado` | HU-09, CU-09, issue #9 |
| PB-011 | Consolidacion funcional de fuentes `.atom` versionadas para oportunidades TI Canarias | La aplicacion debe tomar todos los ficheros `.atom` presentes en `data/`, filtrar Canarias y TI con reglas estructuradas y consolidar una unica version vigente por expediente aunque los snapshots cambien de nombre por fecha y hora. | P0 | Convierte una muestra real de datos versionados en una fuente operativa trazable para el catalogo y evita depender de nombres de fichero estaticos o cargas manuales ambiguas. | 1. La aplicacion procesa todos los ficheros `.atom` de `data/` sin depender de un nombre fijo. 2. Solo se incluyen oportunidades que cumplan criterio geografico Canarias y criterio TI segun reglas estructuradas vigentes. 3. El criterio geografico usa como filtro prioritario `CountrySubentityCode` con prefijo `ES7` y como apoyo `ParentLocatedParty` o `CountrySubentity` con nombres de Canarias o islas canarias. 4. El criterio TI usa CPVs que empiecen por `72`, `48` o `302`. 5. Si un expediente aparece en varios snapshots, el sistema conserva una unica version funcional con el dato mas reciente disponible. 6. Cada registro consolidado conserva el nombre del fichero `.atom` del que procede la version vigente. | PB-009, PB-006 | `cerrado` | HU-11, CU-11, issue #11 |
| PB-012 | Exposicion funcional en la aplicacion del dataset de licitaciones TI Canarias | La aplicacion debe mostrar la informacion consolidada de licitaciones, lotes y adjudicaciones en superficies verificables alineadas con el Excel de referencia y con trazabilidad visible al fichero origen en el detalle. | P0 | Hace verificable para usuario y QA que el producto ya muestra la informacion estructurada util derivada de los `.atom`, no solo una extraccion interna no visible. | 1. La aplicacion muestra una pestaña `Licitaciones TI Canarias`. 2. La aplicacion muestra una pestaña `Detalle Lotes`. 3. La aplicacion muestra una pestaña `Adjudicaciones`. 4. El detalle de licitacion o contrato muestra el nombre del fichero `.atom` origen de la version consolidada. 5. La muestra visible en la aplicacion se corresponde funcionalmente con `data/licitaciones_ti_canarias.xlsx` en esas tres pestañas. 6. Si un campo no viene informado en origen, se muestra como no informado o equivalente claro. | PB-011, PB-002 | `cerrado` | HU-12, CU-12, issue #12 |
| PB-010 | Navegacion principal responsive con menu lateral de iconos | La aplicacion debe ofrecer una estructura de navegacion comun con menu lateral izquierdo de iconos y adaptacion al ancho disponible para sostener el crecimiento de modulos sin perder claridad de uso. | P1 | Mejora orientacion, reduce friccion de uso y prepara una base escalable para catalogo, alertas, pipeline y futuras vistas. | 1. En anchos amplios existe una navegacion principal persistente en el lateral izquierdo con iconos en vertical. 2. La opcion activa se distingue claramente. 3. El contenido principal y la navegacion se adaptan al ancho disponible sin solapamientos ni scroll horizontal estructural. 4. En anchos reducidos la navegacion principal sigue siendo accesible mediante una variante responsive coherente. 5. Las opciones aun no disponibles no se presentan como funcionalidades operativas sin senalizacion. 6. Existe una issue ejecutable para `developer-teams` con trazabilidad a backlog, historia y caso de uso. | PB-001, PB-002, PB-003 | `cerrado` | HU-10, CU-10, issue #10 |
| PB-004 | Configuracion de alertas tempranas | El usuario debe poder definir alertas para recibir nuevas oportunidades relevantes sin busqueda manual recurrente. | P1 | Materializa la promesa diferencial de anticipacion y refuerza la recurrencia de uso. | 1. El usuario puede crear al menos una alerta con un criterio funcional valido entre palabra clave, presupuesto, procedimiento o ubicacion. 2. El sistema impide guardar una alerta vacia y solicita completar al menos un criterio. 3. El sistema deja visible que la alerta esta activa. 4. Cuando aparece una oportunidad que cumple los criterios, queda registrada para notificacion o consumo interno del MVP. 5. El usuario puede editar o desactivar la alerta. 6. Las oportunidades con estado oficial `anulada`, `desierta` o `desistida` no deben registrarse como nuevas coincidencias accionables. | PB-001, PB-003 | `cerrado` | HU-04, CU-04, issue #6 |
| PB-005 | Pipeline de seguimiento de oportunidades | El usuario debe poder guardar oportunidades y moverlas por estados de trabajo para coordinar su respuesta comercial o tecnica. | P1 | Favorece retencion y gestion del trabajo sobre oportunidades detectadas. | 1. El usuario puede guardar una oportunidad en su pipeline desde una superficie visible del catalogo o del detalle. 2. La oportunidad guardada entra por defecto en estado `Nueva`. 3. El usuario puede cambiarla a `Evaluando`, `Preparando oferta`, `Presentada` o `Descartada`. 4. Puede consultar su pipeline con el estado actual de cada oportunidad. 5. El cambio de estado queda reflejado de forma consistente y sin duplicados. 6. Si la fuente oficial marca la oportunidad como `anulada`, `desierta` o `desistida`, el pipeline conserva el registro del usuario y muestra una advertencia visible de estado oficial. | PB-001, PB-002 | `cerrado` | HU-05, CU-05, issue #7 |
| PB-013 | Modelo funcional de roles y permisos | La aplicacion debe aplicar permisos coherentes para `Administrador`, `Colaborador` y `Lector/Invitado` sobre las superficies ya disponibles de consulta, alertas y gobierno funcional, dejando preparada la extension a pipeline cuando `PB-005` exista sin reabrir la matriz de permisos. | P1 | Reduce ambiguedad operativa, prepara crecimiento multiusuario y evita fugas funcionales entre consulta y gestion. | 1. El sistema distingue al menos los roles `Administrador`, `Colaborador` y `Lector/Invitado`. 2. `Administrador` puede consultar, gestionar alertas, consultar KPIs y administrar roles o permisos, ademas de cualquier accion ya disponible para los demas roles. 3. `Colaborador` puede consultar y gestionar sus alertas propias; las reglas de pipeline quedan definidas pero no bloquean esta primera iteracion si `PB-005` aun no esta expuesta. 4. `Lector/Invitado` solo puede consultar catalogo, detalle, filtros y vistas consolidadas. 5. Las acciones no permitidas quedan bloqueadas de forma consistente y no se muestran como disponibles. 6. Existe una issue ejecutable para `developer-teams` con escenarios verificables por rol sobre superficies ya disponibles. | PB-010, PB-004 | `cerrado` | HU-13, CU-13, issue #13 |
| PB-014 | Paginacion de resultados del catalogo | El usuario debe poder recorrer el catalogo y la API en bloques manejables cuando el volumen de licitaciones crezca. | P1 | Mejora la usabilidad de listados amplios y evita degradacion perceptible al consultar resultados numerosos. | 1. El catalogo HTML y la API JSON permiten navegar por paginas. 2. El sistema muestra el total de resultados y el rango actualmente visible. 3. El usuario puede avanzar, retroceder y saltar a una pagina concreta. 4. La paginacion conserva los filtros activos y el orden de listado. 5. Si la pagina solicitada es inexistente o invalida, el sistema responde con un comportamiento controlado y consistente. | PB-001, PB-003 | `cerrado` | HU-14, CU-14, issue #15 |
| PB-015 | Panel de control de conservacion y archivado de licitaciones | La administracion debe poder definir la antiguedad de conservacion de licitaciones y su paso a archivado sin borrar oportunidades con seguimiento activo. | P1 | Reduce costes de almacenamiento y deja una politica clara de retencion y archivo sin perder trazabilidad historica. | 1. Existe una vista de panel de control para configurar la antiguedad en dias. 2. La politica puede expresarse como dias desde la creacion o como antiguedad de cerradas. 3. Las licitaciones con seguimiento activo no se borran nunca. 4. Las licitaciones cerradas que estuvieron activas se mueven a una tabla de archivadas. 5. La tabla archivada conserva los mismos datos que la tabla principal. 6. El panel deja visible que registros se conservaran, archivaran o mantendran activos. | issue #14 integrada en `main`, PB-005 cerrada administrativamente | `nuevo` | HU-15, CU-15, issue #16 |
| PB-016 | Gestion de usuarios y accesos de plataforma | El modulo debe permitir administrar cuentas, roles, permisos y estados de acceso de usuarios de Licican con trazabilidad y baja logica. | P2 | Facilita el gobierno de acceso de la plataforma y evita gestionar cuentas fuera del sistema. | 1. Existe un listado paginado de usuarios con nombre, email, rol, superficies, estado, ultimo acceso y acciones por fila. 2. Se puede crear, editar, activar, desactivar, reactivar, reenviar invitacion y reiniciar acceso. 3. El sistema bloquea emails duplicados y evita dejar la plataforma sin administradores activos. 4. Los cambios de rol, permisos y estado quedan auditados. 5. La baja logica sustituye a la eliminacion fisica. | PB-013, contexto de sesion y trazabilidad de cambios disponibles en `main` | `cerrado` | HU-16, CU-16, issue #28 |
| T-002 | Corregir el filtrado de licitaciones tras PostgreSQL | La aplicacion debe recuperar el filtrado funcional del catalogo despues de la migracion al backend PostgreSQL. | P0 | Restablece una capacidad central del producto y evita una regresion funcional visible para el usuario. | 1. Los filtros por palabra clave, presupuesto, procedimiento y ubicacion funcionan en HTML y API sobre PostgreSQL. 2. Los rangos de presupuesto invalidos siguen devolviendo una respuesta controlada. 3. Los filtros combinados devuelven resultados coherentes con el backend `file` cuando se comparan sobre la misma muestra. 4. El filtrado no depende de datos embebidos obsoletos ni de rutas internas de la implementacion anterior. 5. Existen pruebas de regresion especificas sobre PostgreSQL para la combinacion de filtros mas usada. | PB-003, issue #14 | `cerrado` | CU-03, issue #17 |
| T-003 | Unificar utilidades de texto compartidas | La aplicacion debe centralizar la normalizacion, limpieza y slugificado de texto en un modulo compartido para evitar divergencias entre modulos del dominio. | P1 | Reduce duplicacion, simplifica cambios futuros y baja el riesgo de inconsistencias en catalogo, consolidacion y lectura de datos. | 1. Existe `licican/shared/text.py` con `normalize_text`, `slugify`, `clean_text` y `normalize_optional`. 2. `atom_consolidation.py`, `postgres_catalog.py`, `canarias_dataset.py` y `opportunity_catalog.py` importan esas utilidades desde el modulo comun. 3. No quedan implementaciones duplicadas de normalizacion o slugificado en esos modulos. 4. Se mantienen type hints en funciones tocadas y docstring breve en funciones publicas nuevas. 5. La verificacion automatizada existente sigue pasando o se crea `tests/shared/test_text.py` con cobertura basica suficiente. | Ninguna | `nuevo` | HU-T-003, CU-T-003, issue #18 |
| T-004 | Centralizar constantes de dominio compartidas | La aplicacion debe mover las constantes y mapeos compartidos de Canarias, CPV, estado y procedimiento a una unica fuente de verdad reutilizable. | P1 | Reduce deuda tecnica y evita que reglas de dominio basicas diverjan entre consolidacion y catalogo PostgreSQL. | 1. Existe `licican/shared/domain_constants.py` con `CANARIAS_KEYWORDS`, `TI_CPV_PREFIXES`, `STATUS_LABELS` y `PROCEDURE_LABELS`. 2. El modulo incluye `map_status` y `map_procedure`. 3. `atom_consolidation.py` y `postgres_catalog.py` usan ese modulo en lugar de duplicados locales. 4. Las constantes especificas de ubicacion de PostgreSQL permanecen en `postgres_catalog.py`. 5. La refactorizacion mantiene el comportamiento externo con verificacion automatizada suficiente. | T-003 | `nuevo` | HU-T-004, CU-T-004, issue #19 |
| T-005 | Endurecer configuracion de credenciales PostgreSQL | La aplicacion debe dejar de depender de passwords hardcodeados y exigir configuracion explicita de credenciales sensibles por entorno. | P1 | Reduce riesgo de seguridad operativa y evita perpetuar secretos embebidos en codigo. | 1. `_resolve_database_url()` deja de usar passwords por defecto hardcodeados. 2. Si falta `LICICAN_DATABASE_URL`, `DATABASE_URL` o una configuracion `DB_*` valida con password, se lanza `PostgresCatalogError` con el mensaje acordado. 3. Host, puerto, base de datos y usuario pueden seguir usando defaults no sensibles. 4. No quedan credenciales hardcodeadas en el codigo afectado. 5. Existen pruebas del caso configurado y del error claro por falta de configuracion suficiente. | T-004 | `nuevo` | HU-T-005, CU-T-005, issue #20 |
| T-006 | Extraer resolucion de configuracion a `licican/config.py` | La aplicacion debe centralizar la lectura de entorno y la resolucion de host, puerto, base path y rutas auxiliares en un modulo dedicado. | P1 | Reduce responsabilidades de `app.py`, facilita pruebas y estabiliza la configuracion transversal. | 1. Existe `licican/config.py` con `load_env_file`, `resolve_base_path`, `resolve_port`, `resolve_host`, `resolve_alerts_path` y `normalize_base_path`. 2. La logica extraida mantiene el comportamiento externo actual. 3. `load_env_file()` carga el `.env` solo una vez. 4. Las funciones `resolve_*` llaman a `load_env_file()` solo cuando todavia no se ha cargado configuracion. 5. Hay verificacion automatizada basica del modulo o de su integracion. | T-005 | `nuevo` | HU-T-006, CU-T-006, issue #21 |
| T-007 | Extraer helpers HTTP a `licican/web/responses.py` | La aplicacion debe mover la construccion de respuestas, redirecciones, cuerpos HTML/JSON y lectura de formularios a un modulo HTTP reutilizable. | P1 | Aisla la capa de transporte HTTP y prepara la posterior separacion de plantillas y router. | 1. Existe `licican/web/responses.py` con `send_response`, `send_redirect`, `json_body`, `html_body`, `read_form_data` y `build_url`. 2. `app.py` importa estos helpers desde el modulo nuevo. 3. No cambia la interfaz WSGI ni las URLs existentes. 4. HTML, JSON, redirecciones y lectura de formularios mantienen el mismo comportamiento externo. 5. La entrega deja evidencia automatizada basica suficiente. | T-006 | `nuevo` | HU-T-007, CU-T-007, issue #22 |
| T-008 | Servir el CSS como recurso estatico | La aplicacion debe dejar de incrustar el CSS en la plantilla base y servirlo como fichero estatico versionado. | P1 | Reduce peso de las respuestas HTML, separa presentacion de estructura y prepara la modularizacion de vistas sin cambiar la experiencia visual. | 1. Existe `licican/web/static/style.css` con el CSS actual. 2. La plantilla base enlaza el CSS mediante `<link rel=\"stylesheet\" ...>`. 3. Existe una ruta `/static/<filename>` con content-type adecuado. 4. El estilo visual se mantiene exactamente igual. 5. La verificacion automatizada cubre al menos el enlace HTML y la ruta estatica. | T-007 | `nuevo` | HU-T-008, CU-T-008, issue #23 |
| T-009 | Separar plantillas HTML por vistas | La aplicacion debe mover la generacion de HTML a modulos de plantillas y componentes especializados por vista. | P1 | Reduce el acoplamiento de `app.py`, mejora legibilidad y permite evolucionar vistas sin mezclar routing ni transporte. | 1. Existe `licican/web/templates/` con los modulos y funciones definidos en la iniciativa de refactorizacion. 2. Cada funcion devuelve `str` con HTML a partir de datos ya preparados. 3. `app.py` deja de contener la generacion detallada de estas vistas. 4. Las salidas HTML mantienen el mismo contenido y comportamiento externo. 5. Cada modulo nuevo tiene al menos una prueba basica o cobertura equivalente suficiente. | T-008 | `nuevo` | HU-T-009, CU-T-009, issue #24 |
| T-010 | Crear un router declarativo para la aplicacion WSGI | La aplicacion debe definir sus rutas de forma declarativa y reducir `app.py` a un punto de entrada delgado que delega en handlers. | P1 | Disminuye complejidad estructural, mejora mantenibilidad y deja la entrada principal alineada con una aplicacion WSGI modular. | 1. Existe `licican/web/router.py` con una estructura declarativa de rutas y handlers. 2. `application()` en `app.py` se limita a parsear la request, localizar la ruta y delegar. 3. Se introduce un objeto `Request` simple o equivalente sin romper la interfaz WSGI. 4. Todas las URLs HTML y JSON existentes siguen respondiendo igual. 5. `app.py` queda por debajo de 100 lineas o desarrollo documenta un bloqueo objetivo con evidencia. | T-009 | `nuevo` | HU-T-010, CU-T-010, issue #26 |
| T-011 | Desacoplar `CatalogFilters` hacia `licican/shared/filters.py` | La aplicacion debe mover `CatalogFilters` a un modulo compartido para eliminar dependencias innecesarias entre alertas y catalogo. | P1 | Reduce acoplamiento transversal y deja mas claro el contrato compartido de filtros. | 1. Existe `licican/shared/filters.py` con `CatalogFilters`. 2. `opportunity_catalog.py` y `alerts.py` importan el dataclass desde el modulo nuevo. 3. `alerts.py` deja de depender de `opportunity_catalog.py` solo por este dataclass. 4. El comportamiento externo de filtros y alertas no cambia. 5. Existe al menos una prueba basica del modulo nuevo o cobertura equivalente de integracion. | T-010 | `nuevo` | HU-T-011, CU-T-011, issue #27 |
| T-012 | Extraer helpers de navegacion XML compartidos | La aplicacion debe mover a un modulo comun las utilidades de navegacion XML usadas por la consolidacion ATOM. | P1 | Mejora testeabilidad de la consolidacion y elimina helpers embebidos en modulos de negocio. | 1. Existe `licican/shared/xml_helpers.py` con `find_first`, `find_first_text`, `iter_local`, `iter_texts`, `find_text_by_path` y `local_name`. 2. `atom_consolidation.py` usa estos helpers desde el modulo nuevo. 3. Las funciones nuevas mantienen type hints y docstring breve cuando sean publicas. 4. La consolidacion ATOM mantiene el mismo comportamiento externo y de negocio. 5. El modulo nuevo dispone de al menos una prueba basica o cobertura equivalente suficiente. | T-011 | `nuevo` | HU-T-012, CU-T-012, issue #25 |
| PB-008 | Medicion basica de valor del producto | El producto debe definir como medir cobertura, adopcion y uso de alertas desde las primeras releases. | P2 | Permite evaluar si el producto confirma la vision y orientar iteraciones posteriores. | 1. Existen KPIs basicos definidos por release. 2. Cada KPI tiene definicion, formula, umbral inicial y decision asociada. 3. La definicion documental de KPIs puede prepararse antes de que toda la instrumentacion este disponible. 4. Los KPIs no bloquean el MVP funcional. | PB-001, PB-004 | `nuevo` | HU-08, CU-07, issue #8 |

## Decisiones funcionales vigentes que impactan backlog
- Una alerta del MVP solo es valida si contiene al menos un criterio funcional informado; no deben existir alertas vacias.
- La primera vez que un usuario guarda una oportunidad en pipeline, el estado inicial obligatorio es `Nueva`.
- Un expediente mixto se considera TI para el MVP cuando cumple al menos una de estas condiciones:
  - el objeto principal o el entregable dominante menciona de forma explicita software, sistemas, ciberseguridad, redes, cloud, datos, licencias o infraestructura TIC
  - el CPV principal es tecnologico
  - la parte TI representa una porcion sustancial del valor esperado y es indispensable para cumplir el objeto del contrato
- Si un expediente mixto solo contiene TI accesoria o secundaria y la fuente no aporta evidencia suficiente para considerarla sustancial, debe quedar fuera del catalogo del MVP o tratarse como caso frontera pendiente de refinamiento.
- Cuando una oportunidad pase a `anulada`, `desierta` o `desistida`, el producto debe conservar la referencia historica y mostrar el estado oficial visible; no debe seguir tratandola como oportunidad nueva activa.
- Cuando una oportunidad sea modificada o rectificada, el producto debe mantener la misma referencia funcional si el expediente sigue siendo el mismo y exponer el ultimo dato oficial disponible en variables criticas.
- La priorizacion de fuentes reales oficiales para recopilacion se ordena asi:
  - `Ola 1`: `BOC`.
  - `Ola 2`: `BOP Las Palmas`.
  - `Ola 3`: `BOE`.
- La iteracion de `PB-009` debe reforzar origen y trazabilidad del dato, no abrir todavia alertas, pipeline ni expansion comercial de cobertura.
- La siguiente iteracion operativa debe tomar todos los `.atom` presentes en `data/` aunque cambien fecha y hora en su nombre.
- El filtro geografico prioritario para Canarias en esta iteracion es `CountrySubentityCode` con prefijo `ES7`; si no basta, deben usarse `ParentLocatedParty` y `CountrySubentity` como apoyo.
- La siguiente iteracion operativa considera TI por CPVs con prefijo `72`, `48` o `302`.
- Si un expediente aparece repetido entre snapshots `.atom`, la aplicacion debe conservar una unica version funcional con el dato oficial mas reciente disponible.
- La salida funcional visible debe alinearse con las pestañas `Licitaciones TI Canarias`, `Detalle Lotes` y `Adjudicaciones` del Excel `data/licitaciones_ti_canarias.xlsx`.
- El detalle de licitacion o contrato debe mostrar el nombre del fichero `.atom` origen de la version consolidada.

## Orden recomendado para `developer-teams`
1. T-003
2. T-004
3. T-005
4. T-006
5. T-007
6. T-008
7. T-009
8. T-010
9. T-011
10. T-012
11. PB-015
12. PB-008

## Reserva operativa por deuda tecnica y seguridad
- Producto mantiene reservada la siguiente ventana de priorizacion despues de `PB-012` para las issues tecnicas que `developer-teams` debe crear a partir de los informes de `quality-auditor` y `security-auditor` del 2026-03-28.
- La secuencia tecnica `T-003` a `T-012` ya ha sido creada en GitHub como issues `#18` a `#27` y pasa a ocupar la ventana de refactorizacion preventiva pedida por desarrollo antes de nuevas expansiones funcionales de mayor alcance.
- Ninguna nueva expansion funcional posterior a `PB-012` debe considerarse definitivamente arrancable hasta que estas issues tecnicas y las derivadas de auditoria puedan ordenarse frente a `PB-015` y `PB-008`.
- Ninguna nueva expansion funcional posterior a `PB-012` debe considerarse definitivamente arrancable hasta que estas issues tecnicas, `PB-015` y `PB-008` puedan ordenarse sin bloqueo.
- La reserva minima de capacidad debe absorber primero los hallazgos de severidad `alta` y `media` que afecten a seguridad operativa, robustez de carga de datos o mantenibilidad del modulo principal.
- A fecha 2026-04-02 la issue tecnica `#14` ya esta cerrada administrativamente, por lo que deja de bloquear el arranque de `PB-015`.

## Notas de priorizacion
- `PB-007` y `PB-006` ya cumplieron su objetivo y quedan cerrados administrativamente tras validacion e integracion en `main`.
- `PB-001`, `PB-002` y `PB-003` ya quedaron validados por `qa-teams`; la siguiente prioridad funcional deja de ser retencion y pasa a ser reforzar recopilacion real sobre fuentes oficiales verificables.
- `PB-009` entra como `P0` por su impacto directo en la credibilidad del producto y en la calidad del valor entregado por el catalogo.
- `PB-009` ya quedo validado, integrado y cerrado administrativamente; la siguiente prioridad funcional ejecutable se reordena para introducir `PB-011` y `PB-012` antes de seguir ampliando modulos de interfaz o retencion.
- `PB-011` ya quedo validado, integrado en `main` y cerrado administrativamente; deja de ser dependencia operativa abierta y pasa a base funcional vigente para `PB-012`.
- `PB-012` entro como nueva prioridad `P0` porque la utilidad del dato consolidado debia ser visible y verificable en la aplicacion y no quedarse solo en un proceso interno o en un Excel auxiliar.
- `PB-012` ya queda validado, integrado en `main` y cerrado administrativamente; deja de ser bloqueo operativo para nuevo trabajo.
- `PB-004` ya no forma parte del trabajo pendiente: queda validado, integrado en `main` y cerrado administrativamente.
- `PB-010` ya quedo cerrada administrativamente y deja de competir como siguiente implementacion activa.
- `PB-013` ya quedo validado, integrado y cerrado administrativamente; pasa a formar parte del baseline funcional vigente.
- La issue tecnica `#14` ya fue validada por `qa-teams`, integrada en `main` y cerrada administrativamente; su impacto transversal en catalogo, detalle y robustez de la fuente operativa del dato queda ya absorbido por la entrega vigente.
- `T-002` ya quedo cerrada administrativamente por `product-manager` tras la validacion de `qa-teams` y la evidencia de merge en `main`; deja de formar parte del trabajo abierto.
- `T-003` a `T-012` entran como una cadena tecnica prioritaria y secuencial para refactorizar sin cambio funcional la base actual, reducir el tamaño de `app.py` y dejar la aplicacion preparada para nuevas iteraciones con menor riesgo de regresion.
- `PB-015` pasa a ser la siguiente prioridad funcional elegible porque el seguimiento activo de licitaciones ya esta integrado y `PB-005` ya esta cerrada administrativamente.
- `PB-008` queda como siguiente trabajo documental tras `PB-015`, sin competir con la prioridad funcional abierta de gobierno de retencion.
- `PB-016` deja de ser trabajo funcional pendiente: ya fue validado por `qa-teams`, esta integrado en `main` y queda cerrado administrativamente.
- `PB-008` queda fuera del camino critico del MVP, pero debe prepararse antes de una ampliacion comercial o de cobertura.

## Riesgos y dependencias abiertas
- Sigue abierta la dependencia de negocio sobre si los ayuntamientos deben entrar en la promesa comercial de primera release o mantenerse en fase posterior.
- Existe riesgo de falsa expectativa si la comunicacion comercial usa lenguaje de cobertura total en lugar de cobertura inicial priorizada.
- Existe riesgo de falsos positivos si desarrollo interpreta de forma demasiado laxa el criterio de "porcion sustancial" en expedientes mixtos.
- Existe riesgo de credibilidad si el producto prioriza alertas o pipeline antes de reforzar la recopilacion con fuentes reales oficiales visibles y verificables.
- Existe riesgo de carga incompleta si la aplicacion queda atada a un nombre fijo de fichero `.atom` y no absorbe nuevos snapshots al cambiar fecha y hora.
- Existe riesgo de perdida de trazabilidad si el detalle de la licitacion no muestra el fichero origen de la version consolidada.
- Existe riesgo de entrega funcionalmente ambigua si `PB-009` no distingue prioridad por olas ni deja claro el minimo de metadatos oficiales exigible por oportunidad recopilada.
- Existe riesgo de navegacion fragmentada y de baja usabilidad si se siguen incorporando modulos sin una estructura principal comun y adaptable al ancho de ventana.
- Existe riesgo de permisos inconsistentes si la aplicacion incorpora mas vistas y acciones sin aplicar de forma homogena el modelo de roles ya definido por producto.
- Existe riesgo de perder foco de valor si el rediseño de interfaz desplaza indefinidamente la evolucion de alertas sin una decision explicita de prioridad.
- Existe una dependencia de priorizacion tecnica abierta: los informes de `quality-auditor` y `security-auditor` del 2026-03-28 exigen que `developer-teams` convierta sus hallazgos accionables en issues tecnicas para que producto pueda reservar capacidad y ordenarlas frente a nuevas expansiones funcionales.
- Existe riesgo de planificacion si `product-manager` no mantiene visible que `PB-015` es la siguiente prioridad funcional elegible y que `PB-008` queda despues como trabajo documental de apoyo.
- Existe riesgo operativo si la evolucion posterior del modulo de usuarios no convierte en trabajo trazable las limitaciones conocidas de autenticacion real, proveedor de identidad y auditoria centralizada.

## Preguntas abiertas para siguiente iteracion
- Que umbral comercial debe exigirse para considerar suficiente la cobertura MVP antes de ampliar a ayuntamientos.
- Que indicadores minimos deben fijarse como puerta de inversion posterior en alertas salientes y monetizacion.

# Changelog 2026-04-01

## product-manager

Hora: 06:05:20 UTC

- Reconciliados `product-backlog.md`, `historias-de-usuario.md`, `roadmap.md`, `refinamiento-funcional.md` e `issues-github.md` con el estado real de GitHub a fecha 2026-04-01.
- Actualizado `PB-005` a `validado`, cerrado documentalmente `PB-012` y `PB-010`, y fijada `PB-013` como siguiente issue funcional elegible por delante de `PB-015`.
- Anadida trazabilidad administrativa en la issue #7 y normalizadas issues abiertas de producto para mantener vinculo claro con backlog, historias y casos de uso.

## developer-teams

Hora: 08:01:15 UTC

- Integrada en `main` la rama `developer-teams/issue-7-pipeline-seguimiento` tras la validacion previa de `qa-teams` para completar el cierre operativo de la issue #7.
- Verificada la integracion en `main` con `PYTHONPATH=src python3 -m pytest -q` y `python3 -m py_compile` sobre los modulos del pipeline y la capa web afectada.
- Pendiente a continuacion el `push` de `main`, el borrado de la rama tecnica y la evidencia final en GitHub para que `product-manager` pueda cerrar administrativamente la issue.

## developer-teams

Hora: 08:19:59 UTC

- Implementada en la rama `developer-teams/issue-13-roles-permisos` la primera capa funcional de roles y permisos con contexto temporal para `Administrador`, `Colaborador` y `Lector/Invitado`.
- Anadidos bloqueo coherente en backend, navegacion condicionada por rol, vistas nuevas de `KPIs` y `Permisos`, y filtrado por propietario en alertas y pipeline.
- Publicada la rama remota y dejado el handoff completo a `qa-teams` en la issue #13 tras verificar `PYTHONPATH=src python3 -m pytest -q` y `python3 -m py_compile`.

## qa-teams

Hora: 10:01:46 UTC

- Validada funcionalmente la issue #13 en la rama `developer-teams/issue-13-roles-permisos` con cobertura exploratoria de permisos para `Lector/Invitado`, `Colaborador` y `Administrador`.
- Verificado el bloqueo consistente de rutas operativas para lector, la gestion de alertas y pipeline por contexto propio en colaborador, y la operacion administrativa sobre alertas de otro contexto.
- Registrado el resultado `validado` en GitHub y dejado trazabilidad del cierre funcional sin detectar defectos bloqueantes.

## doc-teams

Hora: 12:04:22 UTC

- Sincronizada la documentacion de usuario, tecnica, administracion, instalacion, despliegue, FAQ, glosario y matriz de trazabilidad con el estado real de `main`.
- Aclarado que `PB-011` sigue soportado por el codigo, pero que esta checkout no versiona snapshots `.atom` en `data/` y usa `data/opportunities.json` como respaldo cuando no se aportan Atom de entrada.
- Actualizada la cifra de pruebas verificadas a `111` y explicitadas las contradicciones vigentes de `PB-012` y `pipeline`.

## agile-coach

Hora: 14:02:33 UTC

- Reforzada `agile-coach/acuerdos-operativos.md` con una regla explicita de minima duplicacion para que las copias derivadas de reglas compartidas se reduzcan a remisiones breves o plantillas minimas.
- Actualizados `AGENTS.md` de raiz y de los roles activos para remitir a la guia canónica y evitar repetir bloques largos de coordinacion, handoffs y cierres.
- Registrado en `analisis-proceso.md`, `mejoras-propuestas.md` y `riesgos-de-coordinacion.md` el problema de mantenimiento que genera la duplicacion de reglas compartidas y la mejora propuesta para reducir deriva documental.

# Changelog 2026-03-30

## product-manager

Hora: 06:03:51 UTC

- Revise `product-manager/vision-product.md`, `AGENTS.md` y `product-manager/AGENTS.md` para reconciliar backlog, roadmap, historias e issues activas con el estado operativo vigente.
- Actualice los artefactos de producto para reflejar que `PB-012` ya esta `validado` por `qa-teams` en la issue #12, pero sigue pendiente de cierre administrativo hasta que `developer-teams` deje evidencia de fusion en `main` y borrado de rama.
- Reordene la siguiente prioridad operativa para que `developer-teams` integre primero `PB-012` antes de iniciar `PB-010` o `PB-013`.
- Añadi en la issue #12 un comentario administrativo con `Bloqueo actual`, `Siguiente responsable`, `Siguiente paso operativo` y `Estado de integracion: pendiente`.

## developer-teams

Hora: 07:25:31 UTC

- Integre en `main` la rama validada `developer-teams/issue-12-pb-012-vistas-excel` para completar la fusion pendiente de `PB-012`.
- Verifique la suite tecnica en `main` y confirme que ya existia una deuda previa ajena a la integracion: `PYTHONPATH=src python3 -m unittest discover -s tests -v` sigue fallando igual que antes del merge.
- Deje preparado el cierre operativo de `PB-012` y el arranque de la issue #14 sobre una nueva rama tecnica.

## product-manager

Hora: 07:35:32 UTC

- Revise la issue tecnica #14 como trabajo activo prioritario y deje trazabilidad en GitHub de que condiciona la siguiente apertura funcional.
- Actualice `product-manager/product-backlog.md`, `product-manager/roadmap.md` y `product-manager/refinamiento-funcional.md` para reflejar el cierre administrativo de `PB-012`, la supervisión de `#14` y que `PB-013` sigue sin arrancar.
- Cerre administrativamente la issue #12 tras verificar que `developer-teams` ya habia dejado evidencia de merge en `main` y borrado de rama.

## developer-teams

Hora: 07:54:51 UTC

- Implemente la carga del catalogo y del detalle desde PostgreSQL mediante `src/podencoti/postgres_catalog.py`, con backend seleccionable por `PODENCOTI_CATALOG_BACKEND`.
- Añadi manejo controlado de error para devolver `503 Service Unavailable` cuando la fuente PostgreSQL no este disponible y documente la configuracion en `README.md`, `docker-compose.yml`, `Dockerfile` y `requirements.txt`.
- Verifique la nueva ruta tecnica con `PYTHONPATH=src python3 -m unittest tests.test_postgres_catalog tests.test_app.ApplicationTests.test_api_returns_controlled_error_when_postgresql_is_unavailable -v`, `PYTHONPATH=src python3 -m compileall src tests` y una comprobacion WSGI real de `/api/oportunidades` contra la base local.

## developer-teams

Hora: 08:05:03 UTC

- Alinee `tests/test_app.py`, `tests/test_alerts.py` y `tests/test_opportunity_catalog.py` con el backend por defecto introducido en la issue #14 y con el seed real versionado en `data/opportunities.json`.
- Deje los tests de regresion del modo `file` separados de los escenarios especificos de `postgres`, evitando dependencias implicitas del dataset historico de 14 expedientes.
- Verifique la entrega tecnica ampliada con `PYTHONPATH=src python3 -m unittest discover -s tests -v` antes del nuevo handoff a `qa-teams`.

## product-manager

Hora: 08:30:43 UTC

- He actualizado el seguimiento de la issue #14 para reflejar que ya esta validada por `qa-teams` y sigue abierta solo a la espera de evidencia de integracion en `main` y borrado de rama.
- He sincronizado `product-manager/product-backlog.md`, `product-manager/refinamiento-funcional.md`, `product-manager/roadmap.md` y `product-manager/issues-github.md` con ese estado operativo para evitar que el trabajo figure como si siguiera en desarrollo.
- He preparado el comentario administrativo de la issue #14 con el bloqueo actual, el siguiente responsable y el siguiente paso operativo.

## qa-teams

Hora: 10:01:08 UTC

- Valide funcionalmente la issue #14 sobre la rama `developer-teams/issue-14-postgresql-licitacion` con la suite completa y una comprobacion WSGI directa sobre la aplicacion.
- Confirme que la aplicacion responde con `200 OK` en `/` y en `/api/oportunidades`, que el catalogo se sirve desde PostgreSQL y que el detalle devuelve payload estructurado para un expediente visible.
- Deje en GitHub el comentario de validacion con el estado operativo `validado` y sin defectos bloqueantes.

## product-manager

Hora: 11:29:49 UTC

- Cerre administrativamente la issue #14 tras verificar la integracion en `main`, actualice el cuerpo de la issue a `Estado operativo: cerrado` y deje constancia en GitHub de que la entrega pasa a `doc-teams` como siguiente responsable.
- Sincronice `product-manager/product-backlog.md`, `product-manager/roadmap.md`, `product-manager/refinamiento-funcional.md` e `product-manager/issues-github.md` para reflejar que la issue tecnica #14 ya no esta pendiente de integracion.

## doc-teams

Hora: 11:29:49 UTC

- Actualice la documentacion de `doc-teams` para reflejar que la issue tecnica #14 ya esta cerrada y que PostgreSQL es el backend operativo por defecto, manteniendo `PODENCOTI_CATALOG_BACKEND=file` como modo de pruebas aisladas.
- Sincronice `README.md`, manuales, guia de despliegue, guia de instalacion y FAQ con la evidencia vigente de `main`, incluyendo el recuento de 59 pruebas automatizadas y la referencia a `DB_*`.

## developer-teams

Hora: 12:31:13 UTC

- Integre en `main` la rama `developer-teams/issue-14-postgresql-licitacion` para completar la entrega tecnica de la issue #14 sobre PostgreSQL.
- Verifique la integracion en `main` con `PYTHONPATH=src python3 -m unittest discover -s tests -v` y `python3 -m compileall src tests`, dejando la suite en verde con 59 pruebas.
- Elimine la rama tecnica en local y en remoto tras completar el merge.

## doc-teams

Hora: 12:02:23 UTC

- Corrigi la documentacion de `doc-teams` para dejar explicito que la evidencia automatizada de `PB-011` no se reproduce todavia en esta checkout porque los snapshots Atom estan en `data/atom/` y el cargador vigente sigue buscando `data/*.atom`.
- Actualice los manuales, la guia de instalacion, la guia de despliegue, la FAQ y la matriz de trazabilidad para que reflejen la regresion actual de la suite, que descubre 54 pruebas y sigue sin quedar en verde.

## product-manager

Hora: 12:48:11 UTC

- Cree la issue #15 para `PB-014 Paginacion de resultados del catalogo`, con criterios de aceptacion para paginar el catalogo y preservar filtros y orden.
- Cree la issue #16 para `PB-015 Panel de control de conservacion y archivado de licitaciones`, incluyendo configuracion por antiguedad, conservacion de seguimiento activo y archivado en tabla homologada.
- Cree la issue #17 para `T-002 Corregir el filtrado de licitaciones tras PostgreSQL`, dejando como prioridad inmediata la revision del filtrado roto tras la migracion a base de datos.
- Actualice `product-manager/product-backlog.md`, `product-manager/historias-de-usuario.md`, `product-manager/casos-de-uso.md`, `product-manager/roadmap.md`, `product-manager/refinamiento-funcional.md` e `product-manager/issues-github.md` para reflejar las nuevas altas y su orden operativo.

## agile-coach

Hora: 14:00:57 UTC

- Alinee la guia canónica de handoff para que `Rol:` forme parte del bloque literal copiable en arranque, entrega, validacion y cierre administrativo.
- Añadi una metrica ligera para detectar comentarios operativos sin prefijo `Rol:` y reducir deriva en las plantillas entre equipos.
- Deje trazado que el riesgo principal seguia siendo la copia incompleta de plantillas, con impacto directo en trazabilidad y auditoria de issues.

## developer-teams

Hora: 17:41:55 UTC

- Tome la issue #17 en la rama `developer-teams/issue-17-t-002-filtrado-postgresql`, actualice su estado a `en desarrollo` y deje despues el handoff completo a `qa-teams` con `Estado operativo: listo para qa`.
- Corregi en `src/licican/postgres_catalog.py` la resolucion de ubicacion para que PostgreSQL conserve etiquetas geograficas mas especificas desde `jerarquia_org` cuando `ubicacion_comunidad` solo aporta `Canarias`, recuperando filtros combinados coherentes con el backend `file`.
- Añadi una prueba de regresion en `tests/test_postgres_catalog.py` para cubrir palabra clave, presupuesto, procedimiento y ubicacion especifica sobre datos PostgreSQL simulados.
- Verifique la entrega con `PYTHONPATH=src python -m unittest tests.test_postgres_catalog -v`, `PYTHONPATH=src python -m unittest tests.test_app.ApplicationTests.test_api_applies_functional_filters_from_query_string -v` y `PYTHONPATH=src python -m unittest discover -s tests -v`, dejando 60 pruebas en verde.

## qa-teams

Hora: 17:53:23 UTC

- Valide la issue #17 sobre la rama `developer-teams/issue-17-t-002-filtrado-postgresql` con la suite completa y una comprobacion de merge limpio de `origin/main` sobre la rama sin conflictos evitables.
- Confirme que el ajuste de `src/licican/postgres_catalog.py` preserva ubicaciones especificas desde `jerarquia_org` y que la prueba nueva cubre el filtro combinado relevante sobre PostgreSQL.
- Deje la issue marcada como `validado` en GitHub y publique el comentario de validacion con la evidencia de pruebas, riesgos y observaciones.

## doc-teams

Hora: 17:57:51 UTC

- Actualice `doc-teams/README.md` y `doc-teams/manual-tecnico.md` para dejar explicito que la issue #17 ya esta validada por `qa-teams` pero sigue pendiente de integracion en `main`, de modo que la correccion PostgreSQL no se documente todavia como comportamiento vigente.
- Mantenga la fotografia documental de `main` alineada con el estado real observable y sin anticipar como estable una mejora que aun no tiene merge efectivo.

## product-manager

Hora: 18:06:10 UTC

- Sincronice `product-manager/product-backlog.md`, `product-manager/issues-github.md`, `product-manager/refinamiento-funcional.md` y `product-manager/roadmap.md` para reflejar que `T-002` ya quedo validado por `qa-teams` pero sigue pendiente de integracion en `main`.
- Deje en la issue #17 el comentario administrativo con `Bloqueo actual`, `Siguiente responsable`, `Siguiente paso operativo` y `Estado de integracion: pendiente`, manteniendola abierta hasta que `developer-teams` acredite merge y borrado de rama.
- No active la issue #15 todavia, porque la issue #17 sigue abierta por cierre administrativo y el propio flujo del repositorio exige conservar visible esa dependencia antes de continuar con el siguiente item funcional.

## developer-teams

Hora: 18:14:49 UTC

- Integre en `main` la rama `developer-teams/issue-17-t-002-filtrado-postgresql` para completar la entrega tecnica de la issue #17.
- Verifique la integracion en `main` con `PYTHONPATH=src python -m unittest discover -s tests -v`, dejando 60 pruebas en verde tras el merge.
- Deje preparado el cierre operativo de la issue con eliminacion de rama tecnica y comentario de integracion en GitHub.

## product-manager

Hora: 18:21:28 UTC

- Cierre administrativamente la issue #17 tras verificar merge en `main`, borrado de rama y evidencia funcional validada.
- Sincronice backlog, roadmap, refinamiento e historial de issues para reflejar que `T-002` ya quedo `cerrado` y deja de formar parte del trabajo abierto.
- Mantenga `PB-014` como siguiente pieza funcional del roadmap, sin activarla todavia en esta misma iteracion.

## developer-teams

Hora: 18:36:41 UTC

- Implemente `PB-014` en la rama `developer-teams/issue-15-paginacion-catalogo`, añadiendo paginacion visible en el catalogo HTML y metadatos paginados en `/api/oportunidades`.
- Normalice solicitudes de pagina invalidas o fuera de rango con comportamiento consistente, preservando filtros activos y enlaces de navegacion entre paginas.
- Añadi pruebas de catalogo y de aplicacion para salto de pagina, rango visible, conservacion de filtros y ajuste controlado de pagina, dejando la suite completa en verde con `PYTHONPATH=src python -m unittest discover -s tests -v` (67 pruebas).

## qa-teams

Hora: 19:27:37 UTC

- Valide la issue #15 sobre la rama `developer-teams/issue-15-paginacion-catalogo` con la suite de catalogo, la suite de aplicacion y la discovery completa, todas en verde con 67 pruebas.
- Confirme que el catalogo HTML y la API JSON muestran pagina actual, total y rango visible, y que la navegacion conserva filtros activos y orden de listado.
- Deje la issue marcada como `validado` en GitHub y publique el comentario de validacion con la evidencia de pruebas, observaciones y riesgo residual de tamano de pagina fijo.

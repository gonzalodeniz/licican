# Changelog 2026-03-30

## product-manager

Hora: 06:03:51 UTC

- Revise `product-manager/vision-product.md`, `AGENTS.md` y `product-manager/AGENTS.md` para reconciliar backlog, roadmap, historias e issues activas con el estado operativo vigente.
- Actualice los artefactos de producto para reflejar que `PB-012` ya esta `validado` por `qa-teams` en la issue #12, pero sigue pendiente de cierre administrativo hasta que `developer-teams` deje evidencia de fusion en `main` y borrado de rama.
- Reordene la siguiente prioridad operativa para que `developer-teams` integre primero `PB-012` antes de iniciar `PB-010` o `PB-013`.
- Añadi en la issue #12 un comentario administrativo con `Bloqueo actual`, `Siguiente responsable`, `Siguiente paso operativo` y `Estado de integracion: pendiente`.

## developer-teams

Hora: 07:25:31 UTC

- Integre en `main` la rama validada `developer-teams/issue-12-pb-012-vistas-excel` para completar la fusion pendiente de `PB-012`.
- Verifique la suite tecnica en `main` y confirme que ya existia una deuda previa ajena a la integracion: `PYTHONPATH=src python3 -m unittest discover -s tests -v` sigue fallando igual que antes del merge.
- Deje preparado el cierre operativo de `PB-012` y el arranque de la issue #14 sobre una nueva rama tecnica.

## product-manager

Hora: 07:35:32 UTC

- Revise la issue tecnica #14 como trabajo activo prioritario y deje trazabilidad en GitHub de que condiciona la siguiente apertura funcional.
- Actualice `product-manager/product-backlog.md`, `product-manager/roadmap.md` y `product-manager/refinamiento-funcional.md` para reflejar el cierre administrativo de `PB-012`, la supervisión de `#14` y que `PB-013` sigue sin arrancar.
- Cerre administrativamente la issue #12 tras verificar que `developer-teams` ya habia dejado evidencia de merge en `main` y borrado de rama.

## developer-teams

Hora: 07:54:51 UTC

- Implemente la carga del catalogo y del detalle desde PostgreSQL mediante `src/podencoti/postgres_catalog.py`, con backend seleccionable por `PODENCOTI_CATALOG_BACKEND`.
- Añadi manejo controlado de error para devolver `503 Service Unavailable` cuando la fuente PostgreSQL no este disponible y documente la configuracion en `README.md`, `docker-compose.yml`, `Dockerfile` y `requirements.txt`.
- Verifique la nueva ruta tecnica con `PYTHONPATH=src python3 -m unittest tests.test_postgres_catalog tests.test_app.ApplicationTests.test_api_returns_controlled_error_when_postgresql_is_unavailable -v`, `PYTHONPATH=src python3 -m compileall src tests` y una comprobacion WSGI real de `/api/oportunidades` contra la base local.

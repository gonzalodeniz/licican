# Changelog 2026-03-31

## product-manager

Hora: 04:55:00 UTC

- Cree en GitHub las issues tecnicas `T-003` a `T-012` como secuencia de refactorizacion sin cambio funcional externo, con trazabilidad operativa a las issues `#18` a `#27`.
- Actualice `product-manager/product-backlog.md` para incorporar la nueva cadena tecnica, sus dependencias, su prioridad relativa y el nuevo orden recomendado para `developer-teams`.
- Actualice `product-manager/issues-github.md` para dejar constancia local de la numeracion real y del alcance resumido de cada issue tecnica creada.

## developer-teams

Hora: 08:07:43 UTC

- Priorice la issue `#7` de pipeline, abri la rama tecnica `developer-teams/issue-7-pipeline-seguimiento` y deje trazabilidad operativa en GitHub con arranque y handoff a `qa-teams`.
- Implemente el pipeline funcional en Python con persistencia JSON, vista HTML `/pipeline`, API `/api/pipeline`, guardado desde catalogo y detalle, cambio de estados de seguimiento y advertencia visible para estados oficiales `anulada`, `desierta` o `desistida`.
- Añadi pruebas tecnicas especificas del pipeline y verifique la entrega con `PYTHONPATH=src python3 -m pytest -q` y `python3 -m py_compile` sobre los modulos nuevos y modificados.
